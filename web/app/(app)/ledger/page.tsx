import { PhaseStub } from "@/components/PhaseStub";

export default function LedgerPage() {
  return (
    <PhaseStub
      title="Ledger"
      summary="Who owes you and who you owe, with a net balance per person, full history, per-entry settle and settle-all."
    />
  );
}
