import { PhaseStub } from "@/components/PhaseStub";

export default function BudgetPage() {
  return (
    <PhaseStub
      title="Budget"
      summary="A row per category with its limit, spend and progress bar — amber past 80%, red past 100% — plus a month switcher for reviewing past months."
    />
  );
}
